# TASK: Ren'Py Manual MVP — перенос одной сцены

## Context
**Project:** voyage-narrative-engine
**Track:** renpy_mvp
**Role:** vne_renpy_adapter
**Phase:** RN1

## Goal
Вручную перенести одну сцену из VNE в Ren'Py проект (`novel/`):
- Сцена: `sauna_extended` (или любая существующая с `personas/kira/`)
- Создать минимальный `.rpy` файл с персонажами, диалогами и выбором.
- Проверить, что Ren'Py запускается без ошибок.

## Relevant Files
- `novel/` (создать/расширить)
- `scenarios/` (читать как source)
- `personas/kira/` (читать для реплик)
- `tools/vne_to_renpy/` (создать для скриптов экспорта)

## Out of Scope
- Автоматический экспорт всех сцен.
- Изменение persona-модулей.
- Web Studio.
- Графика / визуальные ассеты.

## Acceptance Criteria
- [ ] Ren'Py проект запускается (`renpy run novel/` или эквивалент).
- [ ] В сцене присутствуют минимум 2 персонажа (Kira + User).
- [ ] Есть хотя бы один `menu:` с выбором.
- [ ] Диалоги соответствуют canon (не выдуманы).
- [ ] `say` использует `what` на русском языке.
- [ ] Документация `novel/README.md` описывает структуру.

## Rules
- Не изменять persona-модули.
- Не добавлять backend/API.
- Рабочий язык — русский для внутриигрового текста.
- Не использовать пробелы в именах файлов.

## Instructions
1. Выбери сцену из `scenarios/` или `personas/kira/`.
2. Создай `.rpy` с `define`, `label`, `scene`, `show`, `menu`.
3. Запусти Ren'Py и проверь отсутствие ошибок.
4. Задокументируй структуру в `novel/README.md`.
5. Не меняй файлы вне relevant_files.

---
Generated by Voyage Framework v4.0 | VNE Adapter | Task ID: RN1-renpy-mvp
