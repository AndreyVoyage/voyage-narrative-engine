# Loaded only when Vite runs with --mode local-core (see "dev:local" in
# package.json). Selects HttpCharacterClient instead of MockCharacterClient.
# No secrets here -- safe to commit. Requires
# `py tools/character_lab_react_server.py` running separately (fake provider
# only; see that module's docstring).
VITE_CHARACTER_CLIENT=local
